{% extends 'base.html.twig' %}

{% block body %}
<div class="flex-1">
{# Navbar #}
<nav class="bg-gray-900 text-white">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-16">
      
      {# Logo #}
      <div class="flex-shrink-0">
        <a href="#" class="text-2xl font-bold tracking-tight">LOGO</a>
      </div>

      {# Links #}
      <div class="hidden md:flex space-x-6">
        <a href="#" class="hover:text-gray-300 transition">Home</a>
        <a href="#" class="hover:text-gray-300 transition">Price</a>
        <a href="#" class="hover:text-gray-300 transition">About</a>
        <a href="{{path('user_login')}}" class="hover:text-gray-300 transition">Login</a>
      </div>

      {# Mobile Menu Button (optional) #}
      <div class="md:hidden">
        <button type="button" class="text-gray-400 hover:text-white focus:outline-none">
          <!-- use an icon here -->
          <svg class="h-6 w-6" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16"/>
          </svg>
        </button>
      </div>

    </div>
  </div>
</nav>

    
    
    
    
</div>
{% endblock %}